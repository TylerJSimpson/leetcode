SELECT
     employee_id
    ,bonus = CASE   WHEN employee_id % 2 <> 0 AND LEFT(name, 1) <> 'M'
                    THEN  salary
                    ELSE 0  
                    END
FROM
    Employees
ORDER BY
    employee_id
